These codes were wrriten using the Eclipse Embedded CDT. As it has semiposting and needs other tools for debugging but use xPacks and ARM GCC the code is almost identical that bare metal written ued in Keil uVision, but, this tool start the devices with maximum clock frequency; Keil does the opposite (default: minimum freq, e.g. 8MHz). 

Only source files are included. 
